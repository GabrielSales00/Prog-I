/*#include <stdio.h>
int soma(int X, int Y); //Protótipo da função
int main() { // Programa principal
    printf(“Resultado = %d\n”, soma(15, 25));
    return 0;
}
int soma(int X, int Y) // Definição da função. Como deixamos a função no final, usamos o protótipo para definir seus parâmetros.
{
    return (X+Y);
}